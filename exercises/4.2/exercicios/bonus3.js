/* let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
let mult = []

for (let index = 0; index < numbers.length; index += 1) {
    for (let index2 = 1; index2 < index; index2 += 1) {
        let multiplicação = (numbers[index])*(numbers[index2]);
        mult.push(multiplicação)
    }
}

console.log(mult) */